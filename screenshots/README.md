# Screenshots

The images the Cloudron App Store listing shows (`mediaLinks` in
`CloudronVersions.json`). They are of the real thing, not mockups:

| File | What it is |
|---|---|
| `01-web-mirror.png` | the project's own capsule, in a browser, over the HTML mirror |
| `02-install-page.png` | a content page — one gemtext file, rendered |
| `03-gopher.png` | the same content served over Gopher, in a gopher client |
| `04-setup-wizard.png` | `usv init`, the terminal setup wizard |
| `05-lagrange-spartan.png` | the generated colophon over Spartan, in Lagrange |

Replace them when the interface changes; a store listing showing an old
theme is a small lie that costs trust for no reason.
